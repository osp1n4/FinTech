import { describe, it, expect } from 'vitest';

describe('App', () => {
  it('passes basic test', () => {
    // Test básico para verificar que el setup de testing funciona
    expect(true).toBe(true);
  });
});
